<div class="header" role="banner">
    <div class="site-header">
        <div class="title" {{ pages["_meta"].meta.tagline ? ' class="tagline"' }}>
            <a href="{{ base_url }}">
                <img 
                    class="logotype"
                    src="{{ base_url }}/image/audience.jpg"
                    srcset="
                        {{ base_url }}/image/logo.jpg&q=50&w=250&save-as=jpg 250w,
                        {{ base_url }}/image/logo.jpg&q=60&w=400&save-as=jpg 400w"
                    sizes="
                        (max-width: 767px) 400px,
                        250px"
                    alt="Image of Scarlett Hayes Logotype"
                >
            </a>
        </div>
        <div class="nav">
            <div id="nav" role="navigation" tabindex="-1">
                <ul>
                    {% for page in pages(depthOffset=-1) if page.title and not page.hidden %}
                        <li{% if page.id==current_page.id %} class="active" {% endif %}>
                            <a href="{{ page.url }}">{{ page.title }}</a>
                        </li>
                    {% endfor %}
                    <div class="social-header">
                        {% for social in pages["_meta"].meta.social %}
                            <a href="{{ social.url }}" title="{{ social.title }}" role="button">
                                <i class="{{ social.icon }}" aria-hidden="true"></i>
                                <span class="sr-only">{{ social.title }}</span>
                            </a>
                        {% endfor %}
                    </div>
                </ul>
            </div>
        </div>
        <a class="nav-toggle" id="nav-toggle" title="Toggle Menu" role="button" aria-controls="nav" aria-expanded="false"
            tabindex="0">
            <i class="fa fa-bars" aria-hidden="true" id="toggler"></i>
            <span class="sr-only">Toggle Menu</span>
        </a>
    </div>
    
</div>
